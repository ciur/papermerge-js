Papermerge-js
==============

Frontend (js, scss) project for papermerge backend (django).


Usage
==============

1. Make sure you have node.js installed.
2. Run:

    npm install 

In project folder to install deps.

3.  Build static files:

    npm run prod|dev